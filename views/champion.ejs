<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/bootstrap.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col- offset-2">
                <h1 class="display-1"><img src="<%= champion.icon %> " alt=""><%= champion.name %></h1>
                <p><%= champion.title %> </p>
                <% for(let i=0; i<champion.tags.length;i++){ %> 
                    <span class="badge bg-<%= colors[champion.tags[i]] %>"><%= champion.tags[i]  %> </span>                    
                <% } %> 
                <br>
                <br>
                <a href="/delete/<%= champion.id %> " class="btn btn-danger">Delete</a>
            </div>
        </div>
    </div>
</body>
</html>